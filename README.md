# 3D-Filament-Profile-Database-to-Spoolman
Takes a json export from 3D Filament Profile Database (spooldb) and converts it for import into Spoolman

Usage:
Simply export the json (my-spools.json) from 3D Filament Profiles and run the python file!
